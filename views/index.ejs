<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/Public/style.css">
    <title>Document</title>
</head>
<body>
    <%- include('./partials/header') %>
    <ul class="showIndex">
        <%- store.map(store => `
        <li><p id="showFont"> ${store.title}</p>
        <a href="/${store._id}"><img src = "${store.image_url}" width="150px" height="150px"/></a>
        <form class="deleteBut" action="/${store._id}?_method=DELETE" method="POST">
            <input class="deleteBut"  type="submit" value="DELETE" />
        </form>
        <form class="editBut" action="/${store._id}/edit">
            <input class="editBut" type="submit" value="EDIT" />
        </form></li>
        `).join('') %>
    </ul>
</body>
</html>